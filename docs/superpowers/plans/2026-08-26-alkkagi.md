# 알까기 실시간 대전 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 바둑돌을 튕겨 상대 돌을 판 밖으로 내보내는 알까기를, 서버가 물리를 확정하는 온라인 2인 실시간 대전으로 만든다.

**Architecture:** 순수 물리 모듈(`alkkagiPhysics.ts`)을 서버와 클라이언트가 함께 쓴다. 치는 사람은 `{stoneId, vx, vy}` 하나만 보내고 서버가 시뮬레이션을 돌려 결과를 확정한다. 클라이언트는 같은 모듈로 굴러가는 장면을 그리고 끝나면 서버 좌표로 스냅한다. 방·자리·채팅·랭킹은 오목(`app/lib/omok.ts`, `GameLobby`/`GameChat`/`GameRanking`)의 구조를 그대로 따른다.

**Tech Stack:** Next.js 16 (App Router) · React 19 · TypeScript · Supabase(Postgres + Realtime) · SWR · Canvas 2D · `node --test`

**Spec:** `docs/superpowers/specs/2026-08-26-alkkagi-design.md`

## Global Constraints

- 주석·UI 문구·커밋 메시지는 모두 **한국어**. 커밋은 `docs/COMMIT_CONVENTION.md`의 `<type>: <subject>` 한 줄 형식(마침표 없음, 50자 내외).
- 테스트는 `node --experimental-strip-types --test` 로 도는 순수 모듈만 대상(`app/**/*.test.ts`). React 컴포넌트·API 라우트 테스트 하네스는 이 저장소에 없으므로 만들지 않는다.
- 게임 식별자는 `alkkagi`. 테이블 접두사 `alkkagi_`, API 경로 `/api/games/alkkagi/...`, 페이지 경로 `/games/alkkagi/...`.
- DB 쓰기는 전부 서버 `service_role`(`getSupabase()`)로만. `anon`/`authenticated`에는 `REVOKE ALL` 후 참여자 `SELECT`만 `GRANT`.
- 물리 상수(스펙 표와 반드시 일치): 판 640, 돌 반지름 26, 돌 5개씩, dt = 1/240초, 감속 1100 단위/s², 최대 발사 속도 1200 단위/s, 정지 판정 8 단위/s, 반발계수 0.94, 시뮬 상한 8초, 최대 끌 거리 213 논리 단위, 취소 임계 16 논리 단위.
- 턴 제한 45초(`TURN_LIMIT_MS = 45_000`), 서버 판정 여유 `TURN_GRACE_MS = 2_000`.
- 물리 모듈에 `Math.random`·`Date.now`를 쓰지 않는다(결정성이 이 설계의 전제다).

---

### Task 1: 물리 엔진

**Files:**
- Create: `app/lib/alkkagiPhysics.ts`
- Test: `app/lib/alkkagiPhysics.test.ts`

**Interfaces:**
- Consumes: 없음(첫 태스크, 순수 모듈)
- Produces:
  - 상수 `BOARD_SIZE=640`, `STONE_RADIUS=26`, `STONES_PER_SIDE=5`, `DT=1/240`, `FRICTION=1100`, `MAX_SHOT_SPEED=1200`, `MIN_SHOT_SPEED=60`, `STOP_SPEED=8`, `RESTITUTION=0.94`, `MAX_SIM_SECONDS=8`, `MAX_DRAG=213`, `CANCEL_DRAG=16`
  - `type Owner = "black" | "white"`
  - `type Stone = { id: string; owner: Owner; x: number; y: number }`
  - `type Shot = { stoneId: string; vx: number; vy: number }`
  - `type Body = Stone & { vx: number; vy: number; fallen: boolean; fallenAtMs: number | null }`
  - `type ShotResult = { stones: Stone[]; removed: string[]; durationMs: number }`
  - `initialStones(): Stone[]`
  - `toBodies(stones: Stone[]): Body[]`
  - `applyShot(bodies: Body[], shot: Shot): boolean`
  - `advance(bodies: Body[], stepsDone: number, elapsedMs: number): number`
  - `allSettled(bodies: Body[]): boolean`
  - `aliveStones(bodies: Body[]): Stone[]`
  - `simulateShot(stones: Stone[], shot: Shot): ShotResult`
  - `previewPath(stones: Stone[], shot: Shot): { x: number; y: number }[]`
  - `clampShot(shot: Shot): Shot`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`app/lib/alkkagiPhysics.test.ts`:

```ts
import assert from "node:assert/strict";
import { test } from "node:test";
import {
  BOARD_SIZE,
  MAX_SHOT_SPEED,
  STONE_RADIUS,
  STONES_PER_SIDE,
  clampShot,
  initialStones,
  previewPath,
  simulateShot,
  type Stone,
} from "./alkkagiPhysics.ts";

test("시작 배치는 흑백 5개씩이고 모두 판 안에 있다", () => {
  const stones = initialStones();
  assert.equal(stones.length, STONES_PER_SIDE * 2);
  assert.equal(stones.filter((s) => s.owner === "black").length, STONES_PER_SIDE);
  assert.equal(stones.filter((s) => s.owner === "white").length, STONES_PER_SIDE);
  for (const stone of stones) {
    assert.ok(stone.x > STONE_RADIUS && stone.x < BOARD_SIZE - STONE_RADIUS);
    assert.ok(stone.y > STONE_RADIUS && stone.y < BOARD_SIZE - STONE_RADIUS);
  }
  // 흑은 아래(y가 큼), 백은 위
  const black = stones.filter((s) => s.owner === "black")[0];
  const white = stones.filter((s) => s.owner === "white")[0];
  assert.ok(black.y > white.y);
});

test("아무 것도 맞히지 않으면 굴러가다 멈추고 아무도 떨어지지 않는다", () => {
  const stones: Stone[] = [{ id: "b0", owner: "black", x: 320, y: 580 }];
  const result = simulateShot(stones, { stoneId: "b0", vx: 0, vy: -400 });
  assert.equal(result.removed.length, 0);
  assert.equal(result.stones.length, 1);
  assert.ok(result.stones[0].y < 580, "위로 이동했어야 한다");
  assert.ok(result.stones[0].y > STONE_RADIUS, "판 안에 남아야 한다");
  assert.ok(result.durationMs > 0);
});

test("정면으로 맞히면 맞은 돌이 앞으로 나가고 친 돌은 거의 선다", () => {
  const stones: Stone[] = [
    { id: "b0", owner: "black", x: 320, y: 400 },
    { id: "w0", owner: "white", x: 320, y: 300 },
  ];
  const result = simulateShot(stones, { stoneId: "b0", vx: 0, vy: -900 });
  const b0 = result.stones.find((s) => s.id === "b0");
  const w0 = result.stones.find((s) => s.id === "w0");
  assert.ok(w0 === undefined || w0.y < 300, "맞은 돌이 앞으로 밀려야 한다");
  if (b0 && w0) {
    assert.ok(b0.y - w0.y >= 2 * STONE_RADIUS - 1, "두 돌이 겹치면 안 된다");
  }
});

test("판 끝으로 민 돌은 제거되고, 걸친 돌은 살아남는다", () => {
  const out = simulateShot(
    [{ id: "b0", owner: "black", x: 320, y: 100 }],
    { stoneId: "b0", vx: 0, vy: -MAX_SHOT_SPEED },
  );
  assert.deepEqual(out.removed, ["b0"]);
  assert.equal(out.stones.length, 0);

  // 중심이 판 안에 남을 만큼만 민다(중심 기준 판정이므로 반쯤 걸쳐도 산다)
  const edge = simulateShot(
    [{ id: "b0", owner: "black", x: 320, y: 300 }],
    { stoneId: "b0", vx: 0, vy: -700 },
  );
  assert.equal(edge.removed.length, 0);
  const moved = edge.stones[0];
  assert.ok(moved.y >= 0 && moved.y <= BOARD_SIZE);
});

test("같은 입력은 항상 같은 결과를 낸다", () => {
  const stones = initialStones();
  const shot = { stoneId: "b2", vx: 137.5, vy: -880.25 };
  const a = simulateShot(stones, shot);
  const b = simulateShot(stones, shot);
  assert.deepEqual(a, b);
});

test("어떤 샷이든 8초 안에 모두 멈춘다", () => {
  const stones = initialStones();
  for (const angle of [0, 0.4, -0.4, 1.2, -1.2]) {
    const result = simulateShot(stones, {
      stoneId: "b0",
      vx: Math.sin(angle) * MAX_SHOT_SPEED,
      vy: -Math.cos(angle) * MAX_SHOT_SPEED,
    });
    assert.ok(result.durationMs <= 8000, `${angle}: ${result.durationMs}ms`);
  }
});

test("clampShot은 최대 속도를 넘는 샷을 잘라낸다", () => {
  const clamped = clampShot({ stoneId: "b0", vx: 9000, vy: 0 });
  assert.ok(Math.hypot(clamped.vx, clamped.vy) <= MAX_SHOT_SPEED + 1e-6);
  const kept = clampShot({ stoneId: "b0", vx: 300, vy: -400 });
  assert.equal(Math.round(Math.hypot(kept.vx, kept.vy)), 500);
});

test("예측 경로는 첫 충돌에서 멈춘다", () => {
  const stones: Stone[] = [
    { id: "b0", owner: "black", x: 320, y: 500 },
    { id: "w0", owner: "white", x: 320, y: 200 },
  ];
  const path = previewPath(stones, { stoneId: "b0", vx: 0, vy: -MAX_SHOT_SPEED });
  assert.ok(path.length >= 2);
  const last = path[path.length - 1];
  // 흰 돌에 닿는 지점(중심 거리 2R)에서 끊겨야 한다
  assert.ok(last.y - 200 <= 2 * STONE_RADIUS + 10, `마지막 지점 y=${last.y}`);
  assert.ok(last.y > 200, "흰 돌을 지나쳐 그리면 안 된다");
});
```

- [ ] **Step 2: 실패하는지 확인한다**

Run: `npm run test`
Expected: FAIL — `Cannot find module './alkkagiPhysics.ts'`

- [ ] **Step 3: 물리 모듈을 구현한다**

`app/lib/alkkagiPhysics.ts`. 파일 맨 위에 "왜 순수 모듈인지"를 설명하는 주석을 단다(`app/lib/omokForbidden.ts`·`pongConstants.ts`와 같은 톤).

```ts
// 알까기의 판 규격과 물리입니다. DB도 렌더링도 모르는 순수 모듈이라 서버
// (결과 확정)와 클라이언트(굴러가는 장면)가 같은 코드를 그대로 씁니다.
//
// 이 모듈이 결정적이어야 설계 전체가 성립합니다. 서버는 샷 하나를 받아
// 최종 배치를 확정하고, 두 클라이언트는 "발사 전 배치 + 샷"만으로 같은
// 장면을 각자 그립니다. 그래서 Math.random도 Date.now도 쓰지 않고, 돌을
// 처리하는 순서도 id 정렬로 고정합니다.

export const BOARD_SIZE = 640;
export const STONE_RADIUS = 26;
export const STONES_PER_SIDE = 5;

/** 시작 배치: 위아래 변에서 START_MARGIN 들어온 줄에 START_GAP 간격으로. */
const START_MARGIN = 60;
const START_GAP = 128;
const START_X0 = 64;

/** 고정 시뮬 간격. 최대 속도에서 한 스텝 이동이 5단위라 반지름(26)보다
 *  훨씬 작습니다 — 빠른 돌이 다른 돌을 뚫고 지나가지 않습니다. */
export const DT = 1 / 240;
/** 등감속(단위/s^2). 바둑돌은 미끄러지다 서는 마찰이라 지수감쇠보다 이쪽이 맞습니다. */
export const FRICTION = 1100;
/** 최대 파워로 655단위를 굴러가 판(640)을 겨우 가로지릅니다. */
export const MAX_SHOT_SPEED = 1200;
/** 이보다 약한 샷은 손이 미끄러진 것으로 보고 거절합니다(턴을 낭비시키지 않음). */
export const MIN_SHOT_SPEED = 60;
export const STOP_SPEED = 8;
export const RESTITUTION = 0.94;
export const MAX_SIM_SECONDS = 8;
const MAX_STEPS = Math.ceil(MAX_SIM_SECONDS / DT);

/** 화면에서 끌 수 있는 최대 거리(논리 단위)와, 이보다 짧으면 발사를 취소하는 임계. */
export const MAX_DRAG = 213;
export const CANCEL_DRAG = 16;

export type Owner = "black" | "white";
export type Stone = { id: string; owner: Owner; x: number; y: number };
export type Shot = { stoneId: string; vx: number; vy: number };
export type Body = Stone & { vx: number; vy: number; fallen: boolean; fallenAtMs: number | null };
export type ShotResult = { stones: Stone[]; removed: string[]; durationMs: number };

export function initialStones(): Stone[] {
  const stones: Stone[] = [];
  for (let i = 0; i < STONES_PER_SIDE; i += 1) {
    const x = START_X0 + START_GAP * i;
    stones.push({ id: `w${i}`, owner: "white", x, y: START_MARGIN });
    stones.push({ id: `b${i}`, owner: "black", x, y: BOARD_SIZE - START_MARGIN });
  }
  return sortStones(stones);
}

function sortStones<T extends { id: string }>(stones: T[]): T[] {
  return [...stones].sort((a, b) => (a.id < b.id ? -1 : a.id > b.id ? 1 : 0));
}

export function toBodies(stones: Stone[]): Body[] {
  return sortStones(stones).map((stone) => ({ ...stone, vx: 0, vy: 0, fallen: false, fallenAtMs: null }));
}

/** 최대 속도를 넘는 샷을 방향은 그대로 두고 크기만 잘라냅니다. */
export function clampShot(shot: Shot): Shot {
  const speed = Math.hypot(shot.vx, shot.vy);
  if (speed <= MAX_SHOT_SPEED || speed === 0) return shot;
  const k = MAX_SHOT_SPEED / speed;
  return { stoneId: shot.stoneId, vx: shot.vx * k, vy: shot.vy * k };
}

/** 샷의 대상 돌에 속도를 실습니다. 그 돌이 없으면 false. */
export function applyShot(bodies: Body[], shot: Shot): boolean {
  const target = bodies.find((body) => body.id === shot.stoneId && !body.fallen);
  if (!target) return false;
  const clamped = clampShot(shot);
  target.vx = clamped.vx;
  target.vy = clamped.vy;
  return true;
}

/** 한 스텝. stepIndex는 떨어진 시각(fallenAtMs)을 남기는 데만 씁니다. */
function step(bodies: Body[], stepIndex: number): void {
  // 1) 마찰로 감속하고 이동합니다.
  for (const body of bodies) {
    const speed = Math.hypot(body.vx, body.vy);
    if (speed > 0) {
      const next = speed - FRICTION * DT;
      if (next < STOP_SPEED) {
        body.vx = 0;
        body.vy = 0;
      } else {
        const k = next / speed;
        body.vx *= k;
        body.vy *= k;
      }
    }
    body.x += body.vx * DT;
    body.y += body.vy * DT;
  }

  // 2) 판 밖으로 나간 돌을 표시합니다. 중심 기준이라 가장자리에 반쯤 걸친
  //    돌은 살아남습니다. 표시된 순간부터 충돌에서 빠지므로, 떨어지는 돌이
  //    남은 돌을 건드리는 일은 없습니다.
  for (const body of bodies) {
    if (body.fallen) continue;
    if (body.x < 0 || body.x > BOARD_SIZE || body.y < 0 || body.y > BOARD_SIZE) {
      body.fallen = true;
      body.fallenAtMs = stepIndex * DT * 1000;
    }
  }

  // 3) 충돌. 같은 질량 정면 충돌에 반발계수를 적용해 법선 성분만 바꾸고
  //    접선 성분은 그대로 둡니다(비껴 맞으면 얇게 튀는 감각).
  const live = bodies.filter((body) => !body.fallen);
  for (let i = 0; i < live.length; i += 1) {
    for (let j = i + 1; j < live.length; j += 1) {
      const a = live[i];
      const b = live[j];
      const dx = b.x - a.x;
      const dy = b.y - a.y;
      const dist = Math.hypot(dx, dy);
      if (dist === 0 || dist >= STONE_RADIUS * 2) continue;

      const nx = dx / dist;
      const ny = dy / dist;

      // 겹침을 절반씩 밀어 풀어줍니다(안 하면 돌이 서로 파고듭니다).
      const overlap = STONE_RADIUS * 2 - dist;
      a.x -= (nx * overlap) / 2;
      a.y -= (ny * overlap) / 2;
      b.x += (nx * overlap) / 2;
      b.y += (ny * overlap) / 2;

      const va = a.vx * nx + a.vy * ny;
      const vb = b.vx * nx + b.vy * ny;
      if (vb - va >= 0) continue; // 이미 서로 멀어지는 중

      const e = RESTITUTION;
      const nextA = ((1 - e) * va + (1 + e) * vb) / 2;
      const nextB = ((1 + e) * va + (1 - e) * vb) / 2;
      a.vx += (nextA - va) * nx;
      a.vy += (nextA - va) * ny;
      b.vx += (nextB - vb) * nx;
      b.vy += (nextB - vb) * ny;
    }
  }
}

/** 판에 남은 돌이 모두 멈췄는지. 떨어지는 중인 돌은 따지지 않습니다. */
export function allSettled(bodies: Body[]): boolean {
  return bodies.every((body) => body.fallen || (body.vx === 0 && body.vy === 0));
}

export function aliveStones(bodies: Body[]): Stone[] {
  return sortStones(
    bodies.filter((body) => !body.fallen).map((body) => ({ id: body.id, owner: body.owner, x: body.x, y: body.y })),
  );
}

/**
 * 클라이언트 애니메이션용. 지금까지 진행한 스텝 수와 "지금 몇 ms 지점을
 * 그려야 하는지"를 받아 그 지점까지 스텝을 돌리고, 새 스텝 수를 돌려줍니다.
 * 프레임 간격이 들쭉날쭉해도 결과가 흔들리지 않게 고정 스텝으로만 진행합니다.
 */
export function advance(bodies: Body[], stepsDone: number, elapsedMs: number): number {
  const want = Math.min(Math.floor(elapsedMs / (DT * 1000)), MAX_STEPS);
  for (let i = stepsDone; i < want; i += 1) step(bodies, i);
  return Math.max(stepsDone, want);
}

/**
 * 서버가 결과를 확정할 때 쓰는 전체 시뮬레이션입니다. 남은 돌이 모두 멈추면
 * 끝내고, 그 시각을 durationMs로 돌려줍니다(다음 사람의 턴 시계를 이 시각
 * 뒤부터 시작시키는 데 씁니다).
 */
export function simulateShot(stones: Stone[], shot: Shot): ShotResult {
  const bodies = toBodies(stones);
  if (!applyShot(bodies, shot)) {
    return { stones: sortStones(stones), removed: [], durationMs: 0 };
  }

  let steps = 0;
  while (steps < MAX_STEPS && !allSettled(bodies)) {
    step(bodies, steps);
    steps += 1;
  }

  return {
    stones: aliveStones(bodies),
    removed: sortStones(bodies.filter((body) => body.fallen)).map((body) => body.id),
    durationMs: Math.round(steps * DT * 1000),
  };
}

/**
 * 조준 중에 그리는 예측 경로입니다. **첫 충돌 지점까지만** 돌려줍니다 —
 * 끝까지 보여주면 정답을 읽고 클릭하는 게임이 되어버립니다. 맞히려는
 * 의도는 세울 수 있고, 맞은 뒤에 뭐가 어떻게 튀는지는 손끝에 남깁니다.
 */
export function previewPath(stones: Stone[], shot: Shot): { x: number; y: number }[] {
  const bodies = toBodies(stones);
  const shooter = bodies.find((body) => body.id === shot.stoneId);
  if (!shooter) return [];
  const others = bodies.filter((body) => body.id !== shot.stoneId);

  const clamped = clampShot(shot);
  shooter.vx = clamped.vx;
  shooter.vy = clamped.vy;

  const path: { x: number; y: number }[] = [{ x: shooter.x, y: shooter.y }];
  const SAMPLE_EVERY = 6; // 240Hz 시뮬을 40Hz로 솎아 점선으로 그립니다.

  for (let i = 0; i < MAX_STEPS; i += 1) {
    // 다른 돌은 세우고 쏘는 돌만 움직입니다(첫 충돌까지만 필요하므로).
    step([shooter], i);
    if (i % SAMPLE_EVERY === 0) path.push({ x: shooter.x, y: shooter.y });

    if (shooter.fallen) break;
    if (shooter.vx === 0 && shooter.vy === 0) break;

    const hit = others.some(
      (other) => Math.hypot(other.x - shooter.x, other.y - shooter.y) < STONE_RADIUS * 2,
    );
    if (hit) break;
  }

  path.push({ x: shooter.x, y: shooter.y });
  return path;
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `npm run test`
Expected: PASS. 실패하면 **테스트를 고치지 말고** 상수를 의심한다 — 특히 "정면으로 맞히면" 테스트가 실패하면 반발계수와 겹침 해소 순서를 먼저 본다.

- [ ] **Step 5: 커밋**

```bash
git add app/lib/alkkagiPhysics.ts app/lib/alkkagiPhysics.test.ts
git commit -m "feat(알까기): 결정적 물리 엔진과 테스트"
```

---

### Task 2: 턴 시계

**Files:**
- Create: `app/lib/alkkagiMatch.ts`
- Test: `app/lib/alkkagiMatch.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces: `TURN_LIMIT_MS = 45_000`, `TURN_GRACE_MS = 2_000`, `isTurnExpired(turnStartedAt: string | null, now: number): boolean`, `remainingTurnMs(turnStartedAt: string | null, now: number): number`, `swappedColors(room): { black_id, black_name, white_id, white_name }`

`app/lib/badukMatch.ts`와 같은 모양이다. 다른 점은 제한 시간(45초)과, **턴 시작 시각이 미래일 수 있다**는 점 하나다 — 돌이 다 멈추는 시각을 `turn_started_at`에 찍기 때문이다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`app/lib/alkkagiMatch.test.ts`:

```ts
import assert from "node:assert/strict";
import { test } from "node:test";
import { TURN_LIMIT_MS, isTurnExpired, remainingTurnMs, swappedColors } from "./alkkagiMatch.ts";

const NOW = Date.parse("2026-08-26T12:00:00.000Z");
const iso = (offsetMs: number) => new Date(NOW + offsetMs).toISOString();

test("시간이 남아 있으면 초과가 아니다", () => {
  assert.equal(isTurnExpired(iso(-10_000), NOW), false);
});

test("제한 시간과 여유분을 모두 넘기면 초과다", () => {
  assert.equal(isTurnExpired(iso(-(TURN_LIMIT_MS + 2_001)), NOW), true);
});

test("기준 시각이 없으면 초과로 보지 않는다", () => {
  assert.equal(isTurnExpired(null, NOW), false);
});

test("턴 시작이 미래면(돌이 아직 구르는 중) 제한 시간이 그대로 남는다", () => {
  assert.equal(remainingTurnMs(iso(2_000), NOW), TURN_LIMIT_MS);
  assert.equal(isTurnExpired(iso(2_000), NOW), false);
});

test("남은 시간은 0 아래로 내려가지 않는다", () => {
  assert.equal(remainingTurnMs(iso(-999_999), NOW), 0);
});

test("재대국은 흑백을 통째로 맞바꾼다", () => {
  const swapped = swappedColors({
    blackId: "B", blackName: "흑돌", whiteId: "W", whiteName: "백돌",
  });
  assert.deepEqual(swapped, { black_id: "W", black_name: "백돌", white_id: "B", white_name: "흑돌" });
});

test("상대가 없으면 흑백을 바꿀 수 없다", () => {
  assert.throws(() =>
    swappedColors({ blackId: "B", blackName: "흑돌", whiteId: null, whiteName: null }),
  );
});
```

- [ ] **Step 2: 실패하는지 확인한다**

Run: `npm run test`
Expected: FAIL — `Cannot find module './alkkagiMatch.ts'`

- [ ] **Step 3: 구현한다**

`app/lib/badukMatch.ts`를 읽고 같은 구조로 `app/lib/alkkagiMatch.ts`를 쓴다. `TURN_LIMIT_MS = 45_000`으로 두고, 파일 상단 주석에 45초인 이유(조준에 생각할 시간이 필요)와 `turn_started_at`이 미래일 수 있다는 사실을 적는다. `remainingTurnMs`는 `elapsed <= 0`이면 `TURN_LIMIT_MS`를 그대로 돌려주므로 미래 시각이 자연히 처리된다(바둑 구현과 동일). `swappedColors`도 `badukMatch.ts`의 것을 그대로 옮긴다.

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `npm run test`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add app/lib/alkkagiMatch.ts app/lib/alkkagiMatch.test.ts
git commit -m "feat(알까기): 턴 시계 45초와 흑백 교대 규칙"
```

---

### Task 3: DB 마이그레이션

**Files:**
- Create: `supabase/migrations/20260907000000_add_alkkagi.sql`

**Interfaces:**
- Consumes: 없음
- Produces: 테이블 `alkkagi_rooms` / `alkkagi_chat_messages` / `alkkagi_matches` / `alkkagi_ratings`

`supabase/migrations/20260901000000_add_baduk.sql`을 뼈대로 삼되 아래 차이를 반영한다.

- [ ] **Step 1: 마이그레이션을 쓴다**

`alkkagi_rooms`의 판 상태 컬럼(바둑의 `board`/`previous_board` 자리에 들어감):

```sql
    stones JSONB NOT NULL DEFAULT '[]'::jsonb,
    -- 직전 샷을 쏘기 "전"의 배치. last_shot과 짝을 이뤄 재생 필름이 됩니다.
    pre_shot_stones JSONB,
    -- { stoneId, vx, vy }
    last_shot JSONB,
    last_shot_at TIMESTAMPTZ,
    -- 샷마다 +1. 클라이언트가 "이 샷을 이미 봤나" 판단하는 표식이자,
    -- /shoot의 낙관적 동시성 제어 조건입니다.
    shot_seq INTEGER NOT NULL DEFAULT 0,
    shot_count INTEGER NOT NULL DEFAULT 0,
```

나머지는 바둑과 같다: `id`, `room_name`(기본값 `'알까기 방'`), `status TEXT CHECK (status IN ('waiting','playing','finished'))`, `black_id/black_name`, `white_id/white_name`, `turn`, `winner TEXT CHECK (winner IN ('black','white','draw'))`, `started_at`, `turn_started_at`, `rematch_by`, `created_at`, `updated_at`. `idx_alkkagi_rooms_status` 인덱스, RLS 활성화, `REVOKE ALL ... FROM anon, authenticated`, `GRANT SELECT ... TO authenticated`, 참여자 전용 SELECT 정책, `supabase_realtime` 퍼블리케이션 추가(`DO $$ ... EXCEPTION WHEN OTHERS THEN NULL; END $$;` 패턴).

바둑과 다른 점 셋:

1. **`winner`에 `'draw'`를 허용한다** — 마지막 돌끼리 같이 떨어질 수 있다.
2. **채팅 테이블에 `kind`를 처음부터 넣는다** — `supabase/migrations/20260905000000_game_chat_emojis.sql`에서 다른 게임에 `kind`를 어떻게 추가했는지 확인하고 같은 정의를 쓴다(`kind TEXT NOT NULL DEFAULT 'text' CHECK (kind IN ('text','image'))`). `author_role`은 `CHECK (author_role IN ('black','white','spectator'))`.
3. **`alkkagi_ratings`는 주간제다** — 기본키가 `(user_id, week_key)`:

```sql
CREATE TABLE IF NOT EXISTS public.alkkagi_ratings (
    user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    week_key TEXT NOT NULL,
    user_name TEXT NOT NULL,
    rating INTEGER NOT NULL DEFAULT 1200,
    wins INTEGER NOT NULL DEFAULT 0,
    losses INTEGER NOT NULL DEFAULT 0,
    draws INTEGER NOT NULL DEFAULT 0,
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    PRIMARY KEY (user_id, week_key)
);
CREATE INDEX IF NOT EXISTS idx_alkkagi_ratings_rating ON public.alkkagi_ratings(rating DESC);
CREATE INDEX IF NOT EXISTS idx_alkkagi_ratings_week ON public.alkkagi_ratings(week_key);
```

`alkkagi_matches`는 바둑의 것에서 `final_*_score`를 빼고 `winner TEXT NOT NULL CHECK (winner IN ('black','white','draw'))`, `black_left INTEGER NOT NULL`, `white_left INTEGER NOT NULL`(끝났을 때 남은 돌 수 — 전적을 나중에 읽을 때 얼마나 아슬아슬했는지 보인다)로 바꾼다.

파일 맨 위 주석에 "오목·바둑과 같은 4종 구조이고, 판 상태만 좌표 배열"이라는 점과 `stones`/`pre_shot_stones`/`last_shot`/`shot_seq`의 역할을 적는다.

- [ ] **Step 2: SQL 문법을 확인한다**

Supabase 로컬 CLI가 없으므로 눈으로 검증한다. 확인 항목: 모든 `CREATE TABLE`에 `IF NOT EXISTS`가 있는가 / 4개 테이블 모두 `ENABLE ROW LEVEL SECURITY` + `REVOKE ALL`이 있는가 / `alkkagi_rooms`와 `alkkagi_chat_messages`만 `GRANT SELECT` + SELECT 정책 + 퍼블리케이션 추가가 있는가(`matches`·`ratings`는 서버만 읽는다) / 세미콜론 누락이 없는가.

- [ ] **Step 3: 커밋**

```bash
git add supabase/migrations/20260907000000_add_alkkagi.sql
git commit -m "feat(알까기): 방·채팅·전적·주간 랭킹 테이블 추가"
```

---

### Task 4: 서버 방 로직

**Files:**
- Create: `app/lib/alkkagi.ts`, `app/lib/alkkagiChat.ts`, `app/lib/alkkagiRanking.ts`

**Interfaces:**
- Consumes: Task 1의 `initialStones`/`simulateShot`/`clampShot`/`MIN_SHOT_SPEED`/`Stone`/`Shot`, Task 2의 `isTurnExpired`/`swappedColors`, 기존 `gameSeats.ts`의 `resolveLeave`/`resolveSit`, `gameWeek.ts`의 `currentWeekKey`
- Produces:
  - `alkkagi.ts`: `type AlkkagiRoom`(아래 정의), 모듈 내부 전용 `type AlkkagiRoomRow`(DB 스네이크케이스 행)와 `mapRow(row): AlkkagiRoom`, `createRoom`, `listRooms`, `getRoom`, `joinRoom`, `leaveRoom`, `resign`, `claimDisconnectWin`, `requestRematch`, `acceptRematch`, `declineRematch`, `timeoutTurn`, `submitShot(roomId, userId, shot: Shot)`
  - `alkkagiChat.ts`: `type AlkkagiChatAuthorRole`, `type AlkkagiChatMessage`, `getRoomChat`, `postRoomChat`
  - `alkkagiRanking.ts`: `type RankingEntry`, `recordMatchResult(room, winner)`, `getRanking(limit?, weekKey?)`

**`AlkkagiRoom` 타입** (라우트와 클라이언트가 함께 쓴다):

```ts
export type AlkkagiRoom = {
  id: string;
  status: "waiting" | "playing" | "finished";
  roomName: string;
  blackId: string | null;
  blackName: string | null;
  whiteId: string | null;
  whiteName: string | null;
  stones: Stone[];
  preShotStones: Stone[] | null;
  lastShot: Shot | null;
  lastShotAt: string | null;
  shotSeq: number;
  turn: "black" | "white";
  winner: "black" | "white" | "draw" | null;
  shotCount: number;
  startedAt: string | null;
  turnStartedAt: string | null;
  rematchBy: string | null;
  createdAt: string;
};
```

- [ ] **Step 1: `alkkagi.ts`의 뼈대를 오목에서 옮긴다**

`app/lib/omok.ts`를 열어 아래를 **거의 그대로** 옮긴다(테이블 이름과 타입만 바꾼다): `STALE_ROOM_TTL_MS`(24시간), `mapRow`, `createRoom`, `cleanupStaleRooms`, `listRooms`, `getRoom`, `joinRoom`, `finishRoomWithWinner`, `leaveRoom`, `vacateSeat`, `resign`, `claimDisconnectWin`, `requestRematch`, `acceptRematch`, `declineRematch`, `timeoutTurn`.

바꿔야 할 곳:

- `createRoom`의 `board: createEmptyBoard()` → `stones: initialStones()`
- `acceptRematch`의 판 초기화 → `stones: initialStones(), pre_shot_stones: null, last_shot: null, last_shot_at: null, shot_seq: 0, shot_count: 0`
- `finishRoomWithWinner`의 동시성 조건 `.eq("move_count", room.moveCount)` → **`.eq("shot_seq", room.shotSeq)`**. 이유는 오목의 주석과 같다 — 상태를 읽은 시점과 UPDATE 사이에 상대의 샷이 먼저 끝나 승부가 났다면, 낡은 `playing`을 근거로 종료를 덮어써 전적이 두 번 기록되는 것을 막는다.
- `finishRoomWithWinner`의 시그니처를 `winnerColor: "black" | "white" | "draw"`로 넓힌다(무승부가 있다).
- 오목의 금수 관련 import는 전부 뺀다.

- [ ] **Step 2: `submitShot`을 구현한다**

오목의 `submitMove` 자리에 들어가는, 이 게임에서 유일하게 새로운 함수다.

```ts
/**
 * 샷 하나를 받아 결과를 확정합니다. 클라이언트가 보내는 건 "어느 돌을 어느
 * 속도로" 세 값뿐이고, 물리는 서버가 alkkagiPhysics로 다시 돌립니다.
 *
 * 다음 사람의 turn_started_at을 "지금"이 아니라 "돌이 다 멈추는 시각"(미래)
 * 으로 찍는 게 이 함수의 핵심입니다. 내가 쏘는 순간 상대 차례가 시작되는데
 * 상대 화면에서는 돌이 아직 굴러가는 중이라, 그대로 두면 상대는 구경만
 * 하다가 시간을 뺏깁니다. 서버는 시뮬레이션에서 정지 시각을 이미 알고
 * 있으므로 그만큼 시계를 뒤로 미룹니다.
 */
export async function submitShot(
  roomId: string,
  userId: string,
  shot: Shot,
): Promise<{ room: AlkkagiRoom } | { error: string }> {
  const room = await getRoom(roomId);
  if (!room) return { error: "존재하지 않는 방이에요." };
  if (room.status !== "playing") return { error: "진행 중인 게임이 아니에요." };

  const isBlack = room.blackId === userId;
  const isWhite = room.whiteId === userId;
  if (!isBlack && !isWhite) return { error: "참여자만 칠 수 있어요." };

  const myColor: "black" | "white" = isBlack ? "black" : "white";
  if (room.turn !== myColor) return { error: "상대 차례예요." };

  const target = room.stones.find((stone) => stone.id === shot.stoneId);
  if (!target) return { error: "판에 없는 돌이에요." };
  if (target.owner !== myColor) return { error: "내 돌만 칠 수 있어요." };

  const speed = Math.hypot(shot.vx, shot.vy);
  if (!Number.isFinite(speed) || speed < MIN_SHOT_SPEED) {
    return { error: "너무 약해요. 조금 더 끌어서 쳐주세요." };
  }

  const clamped = clampShot({ stoneId: shot.stoneId, vx: shot.vx, vy: shot.vy });
  const result = simulateShot(room.stones, clamped);

  const blackLeft = result.stones.filter((stone) => stone.owner === "black").length;
  const whiteLeft = result.stones.filter((stone) => stone.owner === "white").length;
  const winner: "black" | "white" | "draw" | null =
    blackLeft === 0 && whiteLeft === 0 ? "draw" : whiteLeft === 0 ? "black" : blackLeft === 0 ? "white" : null;
  const finished = winner !== null;

  const supabase = getSupabase();
  if (!supabase) return { error: "서버 오류예요." };

  const now = new Date();
  const nowIso = now.toISOString();
  // 다음 사람의 시계는 돌이 다 멈춘 뒤부터 흐릅니다.
  const nextTurnStartedAt = new Date(now.getTime() + result.durationMs).toISOString();

  // 낙관적 동시성 제어: 읽어온 shot_seq와 지금 DB의 shot_seq가 다르면
  // (그 사이 다른 샷이나 시간 초과 처리가 먼저 끝났으면) 0행에 매치돼
  // 조용히 실패합니다. turn도 함께 걸어 같은 턴에 두 번 쏘는 걸 막습니다.
  const { data, error } = await supabase
    .from("alkkagi_rooms")
    .update({
      stones: result.stones,
      pre_shot_stones: room.stones,
      last_shot: clamped,
      last_shot_at: nowIso,
      shot_seq: room.shotSeq + 1,
      shot_count: room.shotCount + 1,
      turn: myColor === "black" ? "white" : "black",
      status: finished ? "finished" : "playing",
      winner,
      turn_started_at: finished ? null : nextTurnStartedAt,
      rematch_by: finished ? null : room.rematchBy,
      updated_at: nowIso,
    })
    .eq("id", roomId)
    .eq("shot_seq", room.shotSeq)
    .eq("turn", myColor)
    .select()
    .maybeSingle();

  if (error) {
    console.error("submitShot error:", error);
    return { error: "샷을 저장하지 못했어요." };
  }
  if (!data) return { error: "다른 수가 먼저 처리됐어요. 다시 시도해주세요." };

  const updatedRoom = mapRow(data as AlkkagiRoomRow);
  if (finished && updatedRoom.winner) {
    await recordMatchResult(updatedRoom, updatedRoom.winner);
  }
  return { room: updatedRoom };
}
```

- [ ] **Step 3: `alkkagiChat.ts`를 만든다**

`app/lib/omokChat.ts`를 그대로 옮기고 테이블 이름(`alkkagi_chat_messages`)과 타입 이름만 바꾼다. `getRoom`은 `./alkkagi`에서 가져온다.

- [ ] **Step 4: `alkkagiRanking.ts`를 만든다**

`app/lib/omokRanking.ts`를 그대로 옮기고 테이블 이름(`alkkagi_ratings`, `alkkagi_matches`)과 `OmokRoom` → `AlkkagiRoom`만 바꾼다. `recordMatchResult`의 `alkkagi_matches` insert에는 스펙대로 남은 돌 수를 함께 넣는다:

```ts
    black_left: room.stones.filter((stone) => stone.owner === "black").length,
    white_left: room.stones.filter((stone) => stone.owner === "white").length,
```

무승부 처리(`winner === "draw"`)는 오목에 이미 있는 경로를 그대로 쓴다(양쪽 0.5점, `draws + 1`).

- [ ] **Step 5: 타입 검사**

Run: `npm run typecheck`
Expected: 통과. 라우트가 아직 없으므로 이 세 파일이 서로만 참조한다.

- [ ] **Step 6: 커밋**

```bash
git add app/lib/alkkagi.ts app/lib/alkkagiChat.ts app/lib/alkkagiRanking.ts
git commit -m "feat(알까기): 방·샷·채팅·랭킹 서버 로직"
```

---

### Task 5: API 라우트

**Files:**
- Create: `app/api/games/alkkagi/rooms/route.ts`, `app/api/games/alkkagi/rooms/[id]/route.ts`, 그리고 `[id]` 아래 `join/`, `leave/`, `shoot/`, `resign/`, `rematch/`, `timeout/`, `claim-win/`, `chat/` 각각의 `route.ts`, `app/api/games/alkkagi/ranking/route.ts`

**Interfaces:**
- Consumes: Task 4의 모든 export, 기존 `getSession`(`app/lib/auth`), `parseChatBody`(`app/lib/gameChatBody`), `getChatEmojiById`(`app/data/chat-emojis`), `ensurePrevWeekSnapshot`/`getHall`(`app/lib/gameHall`), `currentWeekInfo`(`app/lib/gameWeek`)
- Produces: HTTP 표면. 클라이언트는 `{ room }`, `{ rooms }`, `{ messages }`, `{ message }`, `{ ranking, hall, week }` 모양을 기대한다.

`app/api/games/omok/**`의 같은 이름 파일을 하나씩 열어 import 경로만 바꿔 옮긴다. `rematch/route.ts`는 오목 것을 보고 `action`(`request`/`accept`/`decline`) 분기를 그대로 따른다.

- [ ] **Step 1: `/move` 대신 `/shoot`을 쓴다**

`app/api/games/alkkagi/rooms/[id]/shoot/route.ts`:

```ts
import { NextResponse, type NextRequest } from "next/server";
import { getSession } from "@/app/lib/auth";
import { submitShot } from "@/app/lib/alkkagi";

export async function POST(
  request: NextRequest,
  context: { params: Promise<{ id: string }> },
) {
  const user = await getSession();
  if (!user) {
    return NextResponse.json({ error: "로그인이 필요해요." }, { status: 401 });
  }

  const { id } = await context.params;
  const body = await request.json().catch(() => null);
  const stoneId = body?.stoneId;
  const vx = Number(body?.vx);
  const vy = Number(body?.vy);

  if (typeof stoneId !== "string" || !Number.isFinite(vx) || !Number.isFinite(vy)) {
    return NextResponse.json({ error: "잘못된 요청이에요." }, { status: 400 });
  }

  const result = await submitShot(id, user.id, { stoneId, vx, vy });
  if ("error" in result) {
    return NextResponse.json({ error: result.error }, { status: 400 });
  }
  return NextResponse.json({ room: result.room });
}
```

- [ ] **Step 2: 나머지 라우트를 옮긴다**

오목의 대응 파일을 그대로 옮기되 `@/app/lib/omok` → `@/app/lib/alkkagi`, `@/app/lib/omokChat` → `@/app/lib/alkkagiChat`, `@/app/lib/omokRanking` → `@/app/lib/alkkagiRanking`으로 바꾼다. `ranking/route.ts`의 `ensurePrevWeekSnapshot("omok", ...)`·`getHall("omok")`은 `"alkkagi"`로 바꾼다. 랭킹은 20위에서 자르지 않는다(`getRanking()`을 인자 없이 부른다).

- [ ] **Step 3: 타입 검사와 린트**

Run: `npm run typecheck && npm run lint`
Expected: 둘 다 통과

- [ ] **Step 4: 커밋**

```bash
git add app/api/games/alkkagi
git commit -m "feat(알까기): 방·샷·채팅·랭킹 API 라우트"
```

---

### Task 6: 로비·랭킹·채팅 껍데기와 페이지

**Files:**
- Create: `app/components/moyeobap/AlkkagiLobby.tsx`, `AlkkagiChat.tsx`, `AlkkagiRanking.tsx`
- Create: `app/games/alkkagi/page.tsx`, `app/games/alkkagi/ranking/page.tsx`

**Interfaces:**
- Consumes: 기존 `GameLobby`/`GameChat`/`GameRanking`, Task 5의 API 경로
- Produces: `<AlkkagiLobby />`, `<AlkkagiChat roomId myRole />`(`myRole: 'black' | 'white' | 'spectator'`), `<AlkkagiRanking />`

- [ ] **Step 1: 세 껍데기 컴포넌트를 만든다**

`AlkkagiLobby.tsx` — `OmokLobby.tsx`와 같은 모양. 방장이 흑이다.

```tsx
'use client';

import { GameLobby, type GameLobbyConfig, type LobbyRoomBase } from './GameLobby';

type AlkkagiLobbyRoom = LobbyRoomBase & {
  blackId: string | null;
  blackName: string | null;
  whiteId: string | null;
  whiteName: string | null;
};

// 알까기는 방장이 흑(아래쪽에서 시작·선공), 참여자가 백입니다.
const CONFIG: GameLobbyConfig<AlkkagiLobbyRoom> = {
  apiRooms: '/api/games/alkkagi/rooms',
  pagePath: '/games/alkkagi',
  rankingPath: '/games/alkkagi/ranking',
  namePlaceholder: '방 이름 (예: 점심 전 알까기)',
  hostId: (room) => room.blackId,
  hasOpenSeat: (room) => room.status === 'waiting' && (!room.blackId || !room.whiteId),
};

export function AlkkagiLobby() {
  return <GameLobby config={CONFIG} />;
}
```

`AlkkagiChat.tsx` — `OmokChat.tsx`와 같은 모양. `apiBase: '/api/games/alkkagi/rooms'`, `table: 'alkkagi_chat_messages'`, `channelPrefix: 'alkkagi-chat'`, `roleLabel: { black: '흑', white: '백', spectator: '관전' }`, `emojis: true`(Task 3에서 `kind` 컬럼을 넣었으므로 켠다).

`AlkkagiRanking.tsx` — `OmokRanking.tsx`와 같은 모양. `<GameRanking apiRanking="/api/games/alkkagi/ranking" />`.

- [ ] **Step 2: 로비·랭킹 페이지를 만든다**

`app/games/omok/page.tsx`·`app/games/omok/ranking/page.tsx`와 같은 모양. 문구만 알까기에 맞춘다 — 로비 제목 `🥌 알까기`, 설명 "돌을 튕겨 상대 돌을 판 밖으로 내보내세요.", 랭킹 제목 `🏆 알까기 랭킹`.

- [ ] **Step 3: 타입 검사와 린트**

Run: `npm run typecheck && npm run lint`
Expected: 통과

- [ ] **Step 4: 커밋**

```bash
git add app/components/moyeobap/AlkkagiLobby.tsx app/components/moyeobap/AlkkagiChat.tsx app/components/moyeobap/AlkkagiRanking.tsx app/games/alkkagi
git commit -m "feat(알까기): 로비·랭킹 페이지와 채팅 연결"
```

---

### Task 7: 판 그리기와 드래그 조준

**Files:**
- Create: `app/components/moyeobap/AlkkagiBoard.tsx`

**Interfaces:**
- Consumes: Task 1 전부
- Produces:

```tsx
export type AlkkagiBoardProps = {
  /** 서버가 확정한 현재 배치. 애니메이션이 끝나면 이 값으로 스냅합니다. */
  stones: Stone[];
  /** 재생할 샷. seq가 바뀔 때마다 pre부터 다시 재생합니다. null이면 정지 화면. */
  replay: { seq: number; pre: Stone[]; shot: Shot; startedAtMs: number } | null;
  /** 내 색. 관전자는 'black' 시점으로 봅니다. */
  viewAs: Owner;
  /** 지금 내가 칠 수 있는지(내 차례 + 진행 중 + 재생 중 아님). */
  canShoot: boolean;
  myColor: Owner | null;
  onShoot: (shot: Shot) => void;
  /** 재생이 끝났을 때(턴 표시·시계를 그때 켜기 위해). */
  onReplayEnd?: (seq: number) => void;
};

/** Task 8이 그대로 import해서 쓰는 재생 상태 타입입니다. */
export type Replay = { seq: number; pre: Stone[]; shot: Shot; startedAtMs: number };

export function AlkkagiBoard(props: AlkkagiBoardProps): JSX.Element;
```

- [ ] **Step 1: 좌표 변환과 캔버스 뼈대를 만든다**

캔버스는 `width={BOARD_SIZE} height={BOARD_SIZE}`(논리 좌표 = 캔버스 좌표)로 두고 CSS로만 줄인다(`style={{ width: '100%', maxWidth: 480, aspectRatio: '1 / 1' }}`). 이러면 그리기에서는 좌표 변환이 필요 없고, 포인터 좌표만 논리 좌표로 되돌리면 된다.

**뒤집기는 여기 한 곳에서만 한다.** `viewAs === 'white'`이면 화면 좌표와 논리 좌표가 180° 돌아간다:

```tsx
// 백을 잡은 사람에게는 판을 180도 돌려 보여줍니다. 알까기는 "위로 밀어낸다"는
// 방향감이 조작의 전부라, 내 돌이 위에 있으면 드래그 방향이 계속 헷갈립니다.
// 물리와 DB는 언제나 흑이 아래인 원본 좌표만 씁니다.
const flipped = viewAs === 'white';
function toView(p: { x: number; y: number }) {
  return flipped ? { x: BOARD_SIZE - p.x, y: BOARD_SIZE - p.y } : p;
}
function toLogical(p: { x: number; y: number }) {
  return flipped ? { x: BOARD_SIZE - p.x, y: BOARD_SIZE - p.y } : p;
}
function pointerToLogical(event: React.PointerEvent<HTMLCanvasElement>) {
  const canvas = event.currentTarget;
  const rect = canvas.getBoundingClientRect();
  const x = ((event.clientX - rect.left) / rect.width) * BOARD_SIZE;
  const y = ((event.clientY - rect.top) / rect.height) * BOARD_SIZE;
  return toLogical({ x, y });
}
```

- [ ] **Step 2: 그리기 루프를 만든다**

`requestAnimationFrame` 하나로 조준·재생·정지 화면을 모두 그린다. 그리는 순서:

1. 나무결 판 배경(`#d9b072` 계열 단색 + 미세한 그라디언트)과 옅은 바둑판 격자
2. 돌 — 흑은 `#1a1a1a`, 백은 `#f4f1ea`, 각각 방사형 그라디언트와 아래쪽 그림자. 떨어지는 중인 돌(`fallen`)은 `fallenAtMs` 이후 경과에 따라 600ms에 걸쳐 `globalAlpha`를 1→0, 반지름을 1→0.6배로 줄인다
3. 내 차례이고 조준 중이면 — `previewPath`의 점선(`setLineDash([8, 10])`), 발사 방향 화살표, 돌 아래 파워 게이지

재생 중에는 `Body[]`를 상태가 아니라 `useRef`에 두고 `advance(bodies, stepsDone, elapsedMs)`로 진행시킨다. React 상태로 두면 프레임마다 리렌더가 돌아 애니메이션이 끊긴다.

```tsx
// 상대 화면에서도 같은 시점이 보이도록, 서버가 찍은 발사 시각으로부터
// 이미 지난 시간만큼 건너뛰고 이어서 그립니다. 네트워크가 느렸다고
// 상대 화면에서만 늦게 도착하는 일이 없어집니다.
const elapsedMs = Date.now() - replay.startedAtMs;
stepsRef.current = advance(bodiesRef.current, stepsRef.current, elapsedMs);
if (allSettled(bodiesRef.current)) onReplayEnd?.(replay.seq);
```

재생이 끝나면(모두 정지 + 떨어지는 연출까지 600ms 경과) `stones` prop을 그대로 그리는 정지 화면으로 돌아간다.

- [ ] **Step 3: 드래그 조준을 붙인다**

`onPointerDown`/`onPointerMove`/`onPointerUp`(+ `setPointerCapture`)으로 처리해 트랙패드·마우스·터치가 같은 코드로 동작하게 한다.

```tsx
function handlePointerDown(event: React.PointerEvent<HTMLCanvasElement>) {
  if (!canShoot || !myColor) return;
  const point = pointerToLogical(event);
  // 집기 쉽도록 반지름보다 조금 넉넉하게 잡습니다.
  const picked = stones.find(
    (stone) => stone.owner === myColor && Math.hypot(stone.x - point.x, stone.y - point.y) <= STONE_RADIUS * 1.4,
  );
  if (!picked) return;
  event.currentTarget.setPointerCapture(event.pointerId);
  setAim({ stoneId: picked.id, from: { x: picked.x, y: picked.y }, to: point });
}

function handlePointerUp() {
  if (!aim) return;
  const dx = aim.from.x - aim.to.x; // 끈 반대 방향으로 날아갑니다
  const dy = aim.from.y - aim.to.y;
  const drag = Math.hypot(dx, dy);
  setAim(null);
  if (drag < CANCEL_DRAG) return; // 돌 위로 되돌리면 취소
  const power = Math.min(drag, MAX_DRAG) / MAX_DRAG;
  const speed = MAX_SHOT_SPEED * power;
  onShoot({ stoneId: aim.stoneId, vx: (dx / drag) * speed, vy: (dy / drag) * speed });
}
```

`onPointerMove`는 `aim.to`만 갱신한다. 조준 중에는 캔버스에 `touch-action: none`을 주어 모바일에서 화면이 같이 스크롤되지 않게 한다.

- [ ] **Step 4: 브라우저에서 확인한다**

Run: `npm run dev`
브라우저에서 `/games/alkkagi`로 들어가 방을 만들고, 돌을 끌어 조준선·게이지·예측 점선이 보이는지, 놓았을 때 돌이 굴러가는지 확인한다. 아직 서버 연결은 Task 8에서 붙이므로, 이 단계에서는 콘솔에 샷 값이 찍히는 것까지만 본다.

- [ ] **Step 5: 커밋**

```bash
git add app/components/moyeobap/AlkkagiBoard.tsx
git commit -m "feat(알까기): 판 그리기와 끌어서 놓는 조준"
```

---

### Task 8: 방 컴포넌트와 방 페이지

**Files:**
- Create: `app/components/moyeobap/AlkkagiRoom.tsx`, `app/games/alkkagi/[id]/page.tsx`

**Interfaces:**
- Consumes: Task 7의 `AlkkagiBoard`, Task 6의 `AlkkagiChat`, Task 5의 API, Task 2의 `remainingTurnMs`, 기존 `Spectators`·`useAuth`·`fetcher`·`requestJson`·`createSupabaseBrowserClient`
- Produces: `<AlkkagiRoom roomId />`

`app/components/moyeobap/OmokRoom.tsx`가 뼈대다. 방 상태 SWR(2초 폴링) + `postgres_changes` 구독 + Presence(관전자·상대 접속) + 시계 + 기권/나가기/재대국/몰수승 버튼 묶음을 그대로 옮기고, 보드 자리에 `AlkkagiBoard`를 넣는다. **Presence와 몰수승(60초) 부분은 `PongRoom.tsx`의 것이 더 가깝다** — 두 파일을 같이 열어놓고 옮긴다.

- [ ] **Step 1: 오목 방을 뼈대로 옮긴다**

`OmokRoom.tsx`에서 그대로 가져오는 것: 방 SWR·Realtime 구독·Presence·`statusText` 계산·시계 표시·기권 확인 모달·재대국 신청/수락/거절·빈 자리 앉기·에러 표시. 오목 전용(보드 캔버스 그리기, 금수 hover, `resolveIntersection`)은 전부 뺀다.

- [ ] **Step 2: 재생 상태를 잇는다**

`shotSeq`가 오르면 재생을 시작하고, 이미 본 샷은 건너뛴다.

```tsx
// 새로고침했거나 중간에 들어온 관전자는 지난 샷을 재생하지 않습니다 —
// seq만 맞춰두고 현재 배치부터 봅니다.
const seenSeqRef = useRef<number | null>(null);
const [replay, setReplay] = useState<Replay | null>(null);

useEffect(() => {
  if (!room) return;
  if (seenSeqRef.current === null) {
    seenSeqRef.current = room.shotSeq; // 첫 로드
    return;
  }
  if (room.shotSeq <= seenSeqRef.current) return;
  seenSeqRef.current = room.shotSeq;
  if (!room.preShotStones || !room.lastShot || !room.lastShotAt) return;
  setReplay({
    seq: room.shotSeq,
    pre: room.preShotStones,
    shot: room.lastShot,
    startedAtMs: Date.parse(room.lastShotAt),
  });
}, [room]);
```

- [ ] **Step 3: 낙관적 발사를 붙인다**

```tsx
// 놓는 즉시 내 화면에서 굴러가기 시작하고, 동시에 서버로 보냅니다. 왕복
// 지연을 기다렸다 굴러가면 손맛이 완전히 죽습니다. 물리가 결정적이라
// 서버 결과와 어긋나지 않고, 거절당하면 현재 배치로 되돌립니다.
async function handleShoot(shot: Shot) {
  if (!room) return;
  const localSeq = room.shotSeq + 1;
  seenSeqRef.current = localSeq;
  setReplay({ seq: localSeq, pre: room.stones, shot, startedAtMs: Date.now() });
  try {
    // AlkkagiRoomData는 이 파일 위쪽에 두는 클라이언트용 방 타입입니다.
    // app/lib/alkkagi.ts의 AlkkagiRoom과 같은 모양이되, 서버 전용 모듈을
    // 'use client' 컴포넌트로 끌어오지 않으려고 여기에 다시 적습니다
    // (PongRoom.tsx의 PongRoomData와 같은 방식).
    const response = await requestJson<{ room: AlkkagiRoomData }>(
      `/api/games/alkkagi/rooms/${roomId}/shoot`,
      { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(shot) },
    );
    await mutate({ room: response.room }, { revalidate: false });
  } catch (err) {
    setReplay(null);
    seenSeqRef.current = room.shotSeq;
    setErrorMessage(getErrorMessage(err, '샷을 보내지 못했어요.'));
    await mutate();
  }
}
```

`canShoot`은 `room.status === 'playing' && isMyTurn && replay === null`이다.

- [ ] **Step 4: 남은 돌 수와 상태 문구를 알까기에 맞춘다**

플레이어 줄 아래에 남은 돌 수를 한 줄 넣는다:

```tsx
<p className="alkkagi-room__count">
  ⚫ {room.stones.filter((s) => s.owner === 'black').length}
  {' — '}
  {room.stones.filter((s) => s.owner === 'white').length} ⚪
</p>
```

상태 문구는 오목 것을 쓰되 진행 중 표현만 바꾼다 — 내 차례면 `🟢 내 차례예요. 돌을 끌어서 놓으세요.`, 상대 차례면 `🔴 상대가 조준 중이에요.`

- [ ] **Step 5: 방 페이지를 만든다**

`app/games/pong/[id]/page.tsx`와 같은 모양. 제목 `🥌 알까기`, eyebrow `실시간 대전`, 뒤로 가기 `← 로비로`(`/games/alkkagi`).

- [ ] **Step 6: 타입 검사와 린트**

Run: `npm run typecheck && npm run lint`
Expected: 통과

- [ ] **Step 7: 커밋**

```bash
git add app/components/moyeobap/AlkkagiRoom.tsx "app/games/alkkagi/[id]"
git commit -m "feat(알까기): 방 화면과 샷 실시간 반영"
```

---

### Task 9: 스타일

**Files:**
- Modify: `app/prototype.css`

**Interfaces:**
- Consumes: Task 7·8이 쓰는 클래스 이름
- Produces: `.alkkagi-room-page__layout`, `.alkkagi-room-page__main`, `.alkkagi-room`, `.alkkagi-room__header/__name/__players/__player/__count/__status/__clock/__actions`, `.alkkagi-board`

- [ ] **Step 1: 오목 스타일을 알까기 이름으로 옮긴다**

`app/prototype.css`에서 `.omok-room-page__layout` 이하 블록을 찾아 같은 규칙을 `.alkkagi-*`로 복제한다. 판이 정사각형이라는 점만 다르다:

```css
.alkkagi-board {
  width: 100%;
  max-width: 480px;
  aspect-ratio: 1 / 1;
  border-radius: 8px;
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.18);
  /* 조준 중 모바일에서 화면이 같이 스크롤되지 않게 합니다. */
  touch-action: none;
  cursor: pointer;
}

.alkkagi-room__count {
  font-variant-numeric: tabular-nums;
  letter-spacing: 0.08em;
}
```

랭킹 표는 공용 컴포넌트가 `.omok-ranking__*`를 쓰므로 새로 만들 것이 없다.

- [ ] **Step 2: 브라우저에서 확인한다**

Run: `npm run dev` → `/games/alkkagi`
맥북 화면에서 판과 채팅이 2단으로 놓이는지, 창을 좁혔을 때 판이 넘치지 않는지 본다.

- [ ] **Step 3: 커밋**

```bash
git add app/prototype.css
git commit -m "style(알까기): 방 화면과 판 스타일"
```

---

### Task 10: 게임 목록과 통합 방 목록에 등록

**Files:**
- Modify: `app/games/page.tsx`, `app/lib/openRooms.ts`, `app/lib/openRooms.test.ts`, `app/api/games/rooms/route.ts`

**Interfaces:**
- Consumes: Task 4의 `listRooms`
- Produces: `openRooms.ts`의 `OpenRoomGame`에 `"alkkagi"` 추가, `fromAlkkagi(room: AlkkagiLike): OpenRoom`, `RoomsByGame`에 `alkkagi` 필드 추가

- [ ] **Step 1: 통합 방 목록 테스트를 먼저 쓴다**

`app/lib/openRooms.test.ts`에 오목 케이스를 본떠 추가한다:

```ts
test("알까기 방은 빈 자리가 있을 때만 참여 가능으로 표시된다", () => {
  const waiting = fromAlkkagi({
    id: "a1", roomName: "알까기 방", status: "waiting",
    blackId: "B", whiteId: null, createdAt: "2026-08-26T03:00:00.000Z",
  });
  assert.equal(waiting.game, "alkkagi");
  assert.equal(waiting.href, "/games/alkkagi/a1");
  assert.equal(waiting.joinApi, "/api/games/alkkagi/rooms/a1/join");
  assert.equal(waiting.maxPlayers, 2);
  assert.equal(waiting.playerCount, 1);
  assert.equal(waiting.hasOpenSeat, true);

  const playing = fromAlkkagi({
    id: "a2", roomName: "알까기 방", status: "playing",
    blackId: "B", whiteId: "W", createdAt: "2026-08-26T03:00:00.000Z",
  });
  assert.equal(playing.playerCount, 2);
  assert.equal(playing.hasOpenSeat, false);
});
```

- [ ] **Step 2: 실패하는지 확인한다**

Run: `npm run test`
Expected: FAIL — `fromAlkkagi is not exported`

- [ ] **Step 3: `openRooms.ts`에 알까기를 더한다**

`fromOmok`과 구조가 완전히 같다(2인, 자리 두 칸, 방장이 흑). 네 곳을 고친다: `OpenRoomGame` 유니온, `GAME_LABEL`(`alkkagi: { label: "알까기", emoji: "🥌" }`), `PAGE_PATH`(`alkkagi: "/games/alkkagi"`), `RoomsByGame`과 `mergeOpenRooms`의 배열. `fromAlkkagi`는 `fromOmok`을 그대로 복제하되 `base("alkkagi", ...)`를 쓰고 `meta: null`.

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `npm run test`
Expected: PASS

- [ ] **Step 5: 통합 목록 라우트와 게임 목록에 잇는다**

`app/api/games/rooms/route.ts`에 `import { listRooms as listAlkkagiRooms } from "@/app/lib/alkkagi";`를 더하고 `Promise.all`과 `mergeOpenRooms` 인자에 `alkkagi`를 넣는다.

`app/games/page.tsx`의 `GAMES` 배열에 추가한다(오목 다음, 바둑 앞):

```tsx
  {
    href: '/games/alkkagi',
    emoji: '🥌',
    title: '알까기',
    desc: '돌을 끌어서 튕겨 상대 돌을 판 밖으로 내보내세요. 5개씩, 먼저 다 떨어뜨리면 승리.',
  },
```

- [ ] **Step 6: 전체 검사**

Run: `npm run test && npm run lint && npm run typecheck && npm run build`
Expected: 전부 통과

- [ ] **Step 7: 커밋**

```bash
git add app/games/page.tsx app/lib/openRooms.ts app/lib/openRooms.test.ts app/api/games/rooms/route.ts
git commit -m "feat(알까기): 미니게임 목록과 통합 방 목록에 등록"
```

---

### Task 11: 실제로 쳐보고 손맛 맞추기

**Files:**
- Modify: `app/lib/alkkagiPhysics.ts` (상수만)

물리는 테스트로 잡히지만 손맛은 테스트로 안 잡힌다. 이 태스크는 **반드시 실제로 띄워서 쳐본 뒤에** 끝낸다.

- [ ] **Step 1: 두 브라우저 창으로 한 판 둔다**

Run: `npm run dev`
한 창에서 방을 만들고 다른 창(다른 계정)으로 참여해 끝까지 한 판 둔다. 확인할 것:

1. 최대 파워로 쳤을 때 판을 가로지르는 정도인가 (너무 멀리 나가면 `FRICTION`을 올린다)
2. 맞은 돌이 시원하게 밀려나는가 (덜 밀리면 `RESTITUTION`을 올린다, 상한 0.98)
3. 조준하는 동안 답답하지 않은가 (`MAX_DRAG`가 너무 크면 손목이 아프다)
4. 상대 화면에서 돌이 같은 타이밍에 굴러가는가
5. 돌이 다 멈춘 뒤에 상대 시계가 시작되는가
6. 판 가장자리에 걸친 돌이 살아남는가

- [ ] **Step 2: 상수를 조정하고 테스트를 다시 돌린다**

상수를 바꿨다면 Run: `npm run test`
Expected: PASS. 실패하면 테스트가 아니라 상수 조합이 스펙의 표와 어긋난 것이니, **스펙 문서의 표도 함께 고친다**(문서와 코드가 갈라지면 안 된다).

- [ ] **Step 3: 예외 상황을 확인한다**

- 한 명이 "나가기"를 누르면 방이 남고 자리만 비는가
- 대국 중 나가면 기권패로 기록되는가
- 45초를 넘기면 시간패 처리되는가
- 재대국하면 흑백이 바뀌고 돌이 5개씩 다시 놓이는가
- 채팅과 이모티콘이 되는가
- 랭킹에 결과가 반영되는가
- `/games` 아래 "지금 열려있는 방"에 알까기 방이 뜨는가

- [ ] **Step 4: 커밋**

```bash
git add -A
git commit -m "fix(알까기): 실제로 쳐보고 물리 상수 조정"
```
