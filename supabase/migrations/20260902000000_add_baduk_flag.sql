-- ========================================================
-- 바둑 전용 기능 플래그.
--
-- games_hub 플래그와 별개로 둡니다. 다른 미니게임은 이미 실서버에 공개돼
-- 있는데, 방금 만든 바둑(실시간 대국)만 테스트서버에서 먼저 검증하고
-- 싶기 때문입니다. 기본값은 false(꺼짐)이며, 테스트서버 DB에서만 켭니다.
-- ========================================================

INSERT INTO public.app_flags (key, enabled)
VALUES ('baduk', false)
ON CONFLICT (key) DO NOTHING;
